# AIエージェント向け作業指示

このファイルは、このリポジトリでAIエージェント(Claude Codeなど)に作業を依頼する際の手順・ルールをまとめたものです。人手で自由に書き換えてください。

## note記事(『シン・対話篇』シリーズ等)の編集手順

記事の文体・体裁を編集するよう依頼された場合、以下の手順で進めること。

1. **記事を特定する**: `index.md`から対象の`articles/<3桁番号>_<記事タイトル>/`を特定する。
2. **バックアップを取る**: `<記事ディレクトリ>/backup/`に編集前の`article.md`を保存する。既存バックアップを上書きしない。
3. **体裁・文体のみのレビュー**: 「体裁について感想を」のように内容ではなく見た目を聞かれた場合は、note投稿時の可読性(改行のリズム、太字による拾い読みのしやすさ、見出し構成、スマホでのスクロール量など)に絞って評価し、論旨や主張の中身には触れない。
4. **文体の正規化(依頼があれば実施)**:
   - 短文が空行区切りで連続している箇所は、過剰な空行を削除する。
   - 「会社へ入る。/席に着く。/メールを見る。」のような一行一文の羅列や単語の列挙は、連用中止形(〜し、〜し、〜する)または読点区切りの並列でつなぎ、流れる文章にまとめる。
   - `**太字の結論文**`、`##` 見出し、`---` 区切りは独立した行のまま残す。これらはnote読者が拾い読みする際の目印として機能しているため、地の文に溶け込ませない。
   - 「同期ではない。同期は、人を揃える。秩序は、目的だけを揃える。」のような対比の短文は、「Xではなく、Y」や読点並列に自然な範囲でまとめる。ただし要所の単独一文(修辞的な効き目がある箇所)はそのまま残してよい。

**背景**: この執筆スタイルは、一文一行の断片的な文体で詩的・箴言的なトーンを出すのが基本だが、依頼時にはその断片化を弱め、より一般的な流れる日本語の文章に近づけつつ、太字強調と節区切りによる「拾い読みのしやすさ」は維持したい、という方針。

## `drafts/`下の原稿を正規の記事作成手順へ乗せる方法

`drafts/`下のChatGPTセッション原稿を公開用記事へ変換するよう依頼された場合は、
本文を加工する前に次の順序で正規の記事ディレクトリへ取り込む。

1. **draft原本を確認する**: 対象ファイルが`drafts/`直下にあり、未加工の
   `## Prompt:` / `## Response:`形式であることを確認する。draft原本は編集・移動・削除しない。
2. **記事番号とタイトルを決める**: `articles/`直下の3桁番号の最大値に1を足して次の記事番号とする。
   記事タイトルは原稿先頭のH1から取得し、先頭に`タイトル：`があれば取り除く。
3. **配置先を決める**: `articles/<3桁番号>_<記事タイトル>/`を配置先とする。
   同名のディレクトリまたはファイルが既に存在する場合は上書きせず、作業を止めて確認する。
4. **原本と作業用原稿をコピーする**: 記事ディレクトリとその`backup/`を作成し、
   draft原本を未加工のまま`backup/source.md`へ、同じ内容を加工対象の`article.md`へコピーする。
5. **公開先を決めて通常の変換手順へ進む**: 個人情報・機密情報の候補を確認した後、
   公開先をユーザーに確認して`publication.yml`へ記録する。noteの場合は次節、Zennの場合は
   「Zenn投稿可能なmdに変換する手順」に従って`article.md`を加工する。

この取り込みでは`drafts/`下の原稿が受領時の原本、`backup/source.md`が記事にひも付く
保存用スナップショット、`article.md`が加工後の成果物となる。

## 公開先の選択と記事ごとの記録

1. 公開先をnoteとZennのどちらにするかは、必ずユーザーが判断する。AIエージェントは
   原稿の構文や内容から適性を説明・提案してよいが、ユーザーの明示的な選択なしに決定・変更しない。
2. 選択結果は各記事ディレクトリの`publication.yml`へ、次の形式で記録する。

   ```yaml
   platform: note  # note または zenn
   status: draft   # draft、ready、published
   slug: ""         # Zennの場合に記入
   url: ""         # 公開後に記入
   ```

3. 公開先を変更する場合はユーザーの指示を受けて`platform`を更新し、変更後の公開先に対応する
   成果物を新たに生成する。既存の成果物は無断で削除しない。
4. note用成果物は`article.md`、Zenn用成果物は`article_zenn.md`とする。

## ChatGPTセッションをnote投稿可能なmdに変換する最短手順

ChatGPTの特定セッションをnoteに貼り付けられる状態のmdに変換するよう依頼された場合、
新しいスクリプトを作らず、既存の部品(`replace.sh`, `tools/fix_note_quotes.sh`)を
組み合わせるだけで足りる。以下の順で進めること。

1. **エクスポート(ユーザー作業)**: ユーザーがブラウザのChatGPTエクスポート機能/拡張で
   対象セッションをMarkdownとして書き出す。`tools/chatgpt_session_import.sh`で
   `articles/<3桁番号>_<記事タイトル>/backup/source.md`と`article.md`へ取り込む。
   見出しが `## Prompt:` / `## Response:` で、冒頭に
   `**User:**` / `**Created:**` / `**Updated:**` / `**Exported:**` の
   メタ情報ブロックが付く形式が前提(既存記事はすべてこの形式からの変換)。
2. **話者見出し・体裁の一括置換**: 以下を実行する。
   ```
   replace.sh me,chatgpt,bold,spsp '<file.md>'
   ```
   - `me`: `## Prompt:` → `## 私：`
   - `chatgpt`: `## Response:` → `## ChatGPT:`
   - `bold`: `**太字**` の前後にスペースを補い、地の文と分離する
   - `spsp`: 見出し・区切り線・太字単独行以外の行末にゼロ幅スペースを付与し、
     note/markdownレンダーで改行を保持する
   - 記事中にDARP(道具名)への言及があり、noteの当該記事へのリンクを
     埋め込みたい場合は `darp` も名前リストに加える。
3. **タイムスタンプの削除**: ChatGPT Exporterが冒頭へ付加した
   `**Created:**` / `**Updated:**` / `**Exported:**` の各行と、各話者見出しの直後にある
   `YYYY/M/D H:MM:SS`形式の日時行を削除する。行末にゼロ幅スペースやASCIIスペースが
   付いている場合は、それらも日時行と一緒に削除する。`**User:**`など日時以外の
   メタ情報は、このステップでは削除しない。
4. **数式のプレーンテキスト化**: noteではLaTeX数式ブロックを使用せず、式の意味を保った
   プレーンテキストへ書き換えてfenced code blockにする。分数は`分子 / 分母`、等式は
   `左辺 = 右辺`のように、通常のASCII記号で表す。例えば、
   `\boxed{\text{AI資本効率}=\frac{\text{自己資本}}{\text{AI課金額}}}`は次に変換する。

   ```text
   AI資本効率 = 自己資本 / AI課金額
   ```

5. **引用ブロックの空行対策**:
   ```
   tools/fix_note_quotes.sh '<記事ディレクトリ>/article.md'
   ```
   noteのパーサーは `>` だけの行(空行に見える引用継続行)で引用を打ち切ってしまうため、
   打ち切られないようゼロ幅スペースを挿入する。
6. **タイトル行・ハッシュタグの付与**: 冒頭に `# <タイトル>` を、末尾にハッシュタグ列を
   追加する(手動、またはセッションの最後に `templates/dialog.md` の指示文を投げて
   ボット自身に生成させてもよい)。
7. **表(必要な場合のみ)**: レスポンスに表が含まれる場合は画像化して
   同じ記事ディレクトリに`table1.png`〜`tableN.png`として置く。本文の該当箇所には
   `TODO: tableN.png`と記載し、Markdownの画像リンクは埋め込まない。
8. **タイトル画像指示の準備**: 次のコマンドでテンプレートを記事ディレクトリへコピーする。
   ```
   cp templates/title_image.md '<記事ディレクトリ>/'
   ```
   コピー後、ユーザーが`<記事ディレクトリ>/title_image.md`を編集する。編集が完了するまでタイトル画像を生成せず、ユーザーの指示を待つ。
9. **タイトル画像生成**: ユーザーによる編集後の`<記事ディレクトリ>/title_image.md`を読み、記事のタイトルとシリーズタイトル、
   および同ファイルに記載されたモチーフ・画風・キャラクター指定を反映したタイトル画像を生成する。
   画像は必ず `1280x670` ピクセルであることを確認し、異なる場合はその寸法へ変換する。
   既存の画像を無断で上書きせず、同じ記事ディレクトリの`title.png`として保存する。
10. **レビュー**: GUIツールは起動せず、ユーザーへ`article.md`と関連画像のパスを伝えて最終確認を依頼する。
11. **投稿(ユーザー作業)**: 内容が固まったらユーザーがnoteへ手動で貼り付けて投稿する。
   自動投稿の仕組みはこのリポジトリには無い。

**最短の理由**: 上記はすべて既存スクリプトの呼び出しのみで、新規コードが不要。
ステップ2と5は結果を1コマンドに連結してもよい（その間にステップ3と4を実施する）:
`replace.sh me,chatgpt,bold,spsp '<記事ディレクトリ>/article.md' && tools/fix_note_quotes.sh '<記事ディレクトリ>/article.md'`

## Zenn投稿可能なmdに変換する手順

Zennを公開先としてユーザーが選択した場合は、次の順序で進める。

1. **公開先を確認する**: `<記事ディレクトリ>/publication.yml`の`platform`が`zenn`であることを確認する。
2. **共通本文を整形する**: `article.md`の話者見出しを`## 私：` / `## ChatGPT:`へ変換し、
   太字の前後を正規化する。note専用の`spsp`と`fix_note_quotes.sh`は適用しない。
   既に適用済みの場合は、ゼロ幅スペースをZenn版から除去する。
   fenced code block内にMarkdownの太字指定`**...**`がある場合は、内容を残して
   対になった`**`だけを削除する。既存の`replace.sh`を使う場合は`codebold`を指定し、
   たとえば`replace.sh me,chatgpt,bold,codebold '<file.md>'`とする。Pythonなどの
   `**`演算子を誤って削除しないよう、太字指定として対になっているものだけを対象にする。
   Zenn版ではMarkdown上の改行を維持するため、本文の各非空行の行末へASCIIスペースを
   2個追加する。ただし、Front Matter、fenced code block、`$$`数式ブロックの内部には
   追加せず、コード・設定値・数式の内容を変更しない。既に行末へASCIIスペースが2個以上
   ある場合は追加せず、繰り返し実行してもスペースが増えないようにする。
3. **Zenn版を作る**: `article.md`を基に`article_zenn.md`を作り、先頭へ次のFront Matterを付ける。

   ```yaml
   ---
   title: "記事タイトル"
   emoji: "記事に適した絵文字1文字"
   type: "idea" # tech または idea
   topics: ["topic1", "topic2"] # 最大5件
   published: false
   ---
   ```

   本文先頭の重複するH1、ChatGPT Exporterのメールアドレス・日時メタ情報、末尾の
   `Powered by ChatGPT Exporter`とnote用ハッシュタグ列はZenn版から除去する。
4. **表現を保持する**: fenced code block、`$$`数式ブロック、MermaidブロックはZennが
   ネイティブに扱えるため、画像化せず原文のMarkdownを保持する。
5. **検証する**: コード・数式・Mermaidのブロック数と内容が元原稿から変化していないこと、
   ゼロ幅スペースが残っていないこと、Front Matterが正しいことを確認する。
6. **プレビューする**: Zenn CLIを導入済みならZenn互換の配置へコピーして`npx zenn preview`で確認する。
   未導入の場合は`article_zenn.md`をユーザーへ提示し、ZennのWebエディターで確認を依頼する。
7. **公開する（ユーザー作業）**: 内容が固まったらユーザーがZennのWebエディターまたは
   GitHub連携リポジトリから公開する。公開後は`publication.yml`の`status`と`url`を更新する。

## 個人情報・機密情報の取り扱い

1. 個人情報・機密情報らしき文字列を原稿から抽出する。
2. 抽出した文字列ごとに、「固有名風匿名」（例：東京太郎、Foobar Corp.）への置換案を作成する。
3. 原稿を直接書き換えず、以下の形式で置換候補のリストを提示し、ユーザーの判断を待つ。ユーザーが明示的に承認するまで置換を実行しない。
4. ユーザー本人の個人情報は置換対象としない。ユーザー本人が所有するWebサイト、記事、アカウントなどのURLも、そのまま残す。

```text
1.
from: https://github.com/dominosoft-org/
to: https://github.com/foobar-corporation-org/
2.
from: 牧田さん
to: 山田/鈴木/佐藤さん
```

## データの分類(元データ／中間データ／成果物)

このリポジトリのファイルは、由来と再生成可能性によって3種類に分かれる。
新しいファイルを置く・上書きする前に、どれに当たるかを判断すること。

- **元データ(一次情報)**: ユーザーがブラウザのエクスポート機能で書き出した、
  未加工のChatGPTセッションmd。SSoTはこれ。
  `<記事ディレクトリ>/backup/source.md`に未加工のスナップショットとして残る。
  `backup/`にあるものが元データ、同じ記事ディレクトリの`article.md`が
  加工後の成果物、という対応関係になる。
- **中間データ(再生成可能・使い捨て)**: 対応記事を特定できない作業用画像など。
  `trash/`へ置き、記事ディレクトリへ混入させない。
- **成果物**: `articles/<3桁番号>_<記事タイトル>/article.md`（整形・ハッシュタグ付与まで
  完了した状態）と、同じディレクトリの`title.png`、`table*.png`、英語版など。
  ユーザーがnoteへ手動で貼り付けて投稿した内容の記録であり、
  実際の公開版はnote.com側(外部)にある。このファイル自体は
  「何を投稿したか」の履歴として残す。

判断に迷ったら、「ユーザーの手を離れて外部(ブラウザのエクスポート)から来たか」→元データ、
「対応記事を特定できるか」→記事ディレクトリ、「特定できないか」→`trash/`、
「投稿した内容そのものの記録か」→成果物、
の順で聞く。

## note 記事編集手順の追加
引用ブロック内(先頭限定かも)に ### があると、note のウェブエディタにペーストしたとき、そのブロックの内容が丸ごと消える、あるいはブロックの外に出てしまう現象を観測した。
例：
```
> ### 物理ポインターとは​  
> 情報システムが管理するオブジェクトと、現実世界の物理オブジェクトを結び付ける、人間・機械双方から読み取り可能な参照情報です。​  
```
対策: 引用ブロック内の行頭の # の一個以上の連続はその行を太字にせよという指示に読み替える。
