# AIエージェント向け作業指示

このファイルは、このリポジトリでAIエージェント(Claude Codeなど)に作業を依頼する際の手順・ルールをまとめたものです。人手で自由に書き換えてください。

## note記事(『シン・対話篇』シリーズ等)の編集手順

記事の文体・体裁を編集するよう依頼された場合、以下の手順で進めること。

1. **バックアップを取る**: `./backup/` がなければ作成し、編集対象ファイルをコピーしておく。元の文章と比較できるようにするため。
2. **エディタで開く**: `vscode.sh '<ファイル名>'` を実行し、ユーザーがVS Code上で編集を確認できるようにする。
3. **体裁・文体のみのレビュー**: 「体裁について感想を」のように内容ではなく見た目を聞かれた場合は、note投稿時の可読性(改行のリズム、太字による拾い読みのしやすさ、見出し構成、スマホでのスクロール量など)に絞って評価し、論旨や主張の中身には触れない。
4. **文体の正規化(依頼があれば実施)**:
   - 短文が空行区切りで連続している箇所は、過剰な空行を削除する。
   - 「会社へ入る。/席に着く。/メールを見る。」のような一行一文の羅列や単語の列挙は、連用中止形(〜し、〜し、〜する)または読点区切りの並列でつなぎ、流れる文章にまとめる。
   - `**太字の結論文**`、`##` 見出し、`---` 区切りは独立した行のまま残す。これらはnote読者が拾い読みする際の目印として機能しているため、地の文に溶け込ませない。
   - 「同期ではない。同期は、人を揃える。秩序は、目的だけを揃える。」のような対比の短文は、「Xではなく、Y」や読点並列に自然な範囲でまとめる。ただし要所の単独一文(修辞的な効き目がある箇所)はそのまま残してよい。

**背景**: この執筆スタイルは、一文一行の断片的な文体で詩的・箴言的なトーンを出すのが基本だが、依頼時にはその断片化を弱め、より一般的な流れる日本語の文章に近づけつつ、太字強調と節区切りによる「拾い読みのしやすさ」は維持したい、という方針。

## ChatGPTセッションをnote投稿可能なmdに変換する最短手順

ChatGPTの特定セッションをnoteに貼り付けられる状態のmdに変換するよう依頼された場合、
新しいスクリプトを作らず、既存の部品(`replace.sh`, `fix_note_quotes.sh`, `vscode.sh`)を
組み合わせるだけで足りる。以下の順で進めること。

1. **エクスポート(ユーザー作業)**: ユーザーがブラウザのChatGPTエクスポート機能/拡張で
   対象セッションをMarkdownとして書き出し、このリポジトリ直下に保存する。
   見出しが `## Prompt:` / `## Response:` で、冒頭に
   `**User:**` / `**Created:**` / `**Updated:**` / `**Exported:**` の
   メタ情報ブロックが付く形式が前提(既存記事はすべてこの形式からの変換)。
2. **話者見出し・体裁の一括置換**: 以下を実行する。
   ```
   replace.sh me,chatgpt,bold,spsp '<file.md>'
   ```
   - `me`: `## Prompt:` → `## 私：`
   - `chatgpt`: `## Response:` → `## ChatGPT:`
   - `bold`: `**太字**` の前後にスペースを補い、地の文と分離する
   - `spsp`: 見出し・区切り線・太字単独行以外の行末にゼロ幅スペースを付与し、
     note/markdownレンダーで改行を保持する
   - 記事中にDARP(道具名)への言及があり、noteの当該記事へのリンクを
     埋め込みたい場合は `darp` も名前リストに加える。
3. **引用ブロックの空行対策**:
   ```
   fix_note_quotes.sh '<file.md>'
   ```
   noteのパーサーは `>` だけの行(空行に見える引用継続行)で引用を打ち切ってしまうため、
   打ち切られないようゼロ幅スペースを挿入する。
4. **タイトル行・ハッシュタグの付与**: 冒頭に `# <タイトル>` を、末尾にハッシュタグ列を
   追加する(手動、またはセッションの最後に `templates/dialog.md` の指示文を投げて
   ボット自身に生成させてもよい)。
5. **表・タイトル画像(必要な場合のみ)**: レスポンスに表が含まれる場合は画像化して
   `tables/` に `table1.png`〜`tableN.png` として置き、本文の該当箇所に埋め込む。
   タイトル画像が必要なら `templates/title_image.md` の要領で生成する。
6. **目視レビュー**: `vscode.sh '<file.md>'` で開き、ユーザーが最終確認する
   (体裁レビューの観点は上の「note記事の編集手順」を参照)。
7. **投稿(ユーザー作業)**: 内容が固まったらユーザーがnoteへ手動で貼り付けて投稿する。
   自動投稿の仕組みはこのリポジトリには無い。

**最短の理由**: 上記はすべて既存スクリプトの呼び出しのみで、新規コードが不要。
ステップ2と3は結果を1コマンドに連結してもよい:
`replace.sh me,chatgpt,bold,spsp '<file.md>' && fix_note_quotes.sh '<file.md>'`
